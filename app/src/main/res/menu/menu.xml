<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/manage_order_navigatioBar_widget"
        android:icon="@mipmap/manage_orders"
        android:enabled="true"
        tools:ignore="MenuTitle" />

    <item android:id="@+id/new_order_navigatioBar_widget"
        android:icon="@mipmap/add_new_item"
        android:enabled="true"

        tools:ignore="MenuTitle" />

    <item android:id="@+id/settings_navigatioBar_widget"
        android:icon="@mipmap/settings"
        android:enabled="true"

        tools:ignore="MenuTitle" />
</menu>